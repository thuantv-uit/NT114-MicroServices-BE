{
    "name": "thunio-card-service",
    "version": "1.0.0",
    "description": "Card Service for thunio-like app",
    "main": "app.js",
    "scripts": {
        "start": "node app.js",
        "build": "mkdir -p dist && cp -r *.js package.json dist/",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:unit": "jest --config jest.config.js",
        "test:int": "jest --config jest.int.config.js"
    },
    "dependencies": {
        "axios": "^1.6.0",
        "cloudinary": "^2.7.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2",
        "joi": "^17.9.2",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^7.0.0",
        "multer": "^2.0.1",
        "streamifier": "^0.1.1"
    },
    "devDependencies": {
        "jest": "^30.2.0"
    }
}
